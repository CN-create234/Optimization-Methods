# 最优化方法·核心题型精讲

> 按考研/期末考试**出题频率与分值权重**，将题型分为★★★（必考大题）、★★（高频小题）、★（中频小题），逐类配典型例题，每题下方直接给出详细解答。


## 一、★★★ 必考大题（约50~60分）


### 题型1：线性规划——单纯形法

**出题频率**：★★★★★（每年必考）
**分值**：15~20分
**难度**：★★★★☆

**【例题1】** 用单纯形法求解下列线性规划问题：

$$
\begin{aligned}
\min \quad & z = -3x_1 - 5x_2 \\
\text{s.t.} \quad & x_1 + 2x_2 \leq 6 \\
& 2x_1 + x_2 \leq 6 \\
& x_1, x_2 \geq 0
\end{aligned}
$$

**【详解】**

**第一步**：化为标准形（添加松弛变量）：

$$
\min z = -3x_1 - 5x_2
$$
$$
\text{s.t.} \quad x_1 + 2x_2 + x_3 = 6
$$
$$
\quad 2x_1 + x_2 + x_4 = 6
$$
$$
\quad x_1, x_2, x_3, x_4 \geq 0
$$

**第二步**：列初始单纯形表（基变量 x₃，x₄）：

| 基变量 | x₁ | x₂ | x₃ | x₄ | b |
|--------|----|----|----|----|---|
| x₃ | 1 | 2 | 1 | 0 | 6 |
| x₄ | 2 | 1 | 0 | 1 | 6 |
| z | -3 | -5 | 0 | 0 | 0 |

检验数：σ₁ = -3，σ₂ = -5（均 < 0，需迭代）。**选 x₂ 入基**（σ₂ 最负）。

**第三步**：确定离基变量。计算比值：
- x₃行：6/2 = 3
- x₄行：6/1 = 6
最小比值为3，**x₃ 离基**，主元为2。

**第四步**：进行高斯消元（主元列变为单位向量）：

| 基变量 | x₁ | x₂ | x₃ | x₄ | b |
|--------|----|----|----|----|---|
| x₂ | 1/2 | 1 | 1/2 | 0 | 3 |
| x₄ | 3/2 | 0 | -1/2 | 1 | 3 |
| z | -1/2 | 0 | 5/2 | 0 | 15 |

**第五步**：检查检验数：σ₁ = -1/2 < 0，继续迭代。**选 x₁ 入基**。

计算比值：
- x₂行：3/(1/2) = 6
- x₄行：3/(3/2) = 2
最小比值为2，**x₄ 离基**，主元为3/2。

**第六步**：高斯消元：

| 基变量 | x₁ | x₂ | x₃ | x₄ | b |
|--------|----|----|----|----|---|
| x₂ | 0 | 1 | 2/3 | -1/3 | 2 |
| x₁ | 1 | 0 | -1/3 | 2/3 | 2 |
| z | 0 | 0 | 7/3 | 1/3 | 16 |

**第七步**：所有检验数 σ ≥ 0，最优！

**最优解**：x₁ = 2，x₂ = 2，z_min = -16（原问题最优值 z = -16，即最大化问题最优值为16）。

**【给分要点】**
- 标准形转换（2分）
- 初始单纯形表正确（3分）
- 每次迭代选入基/离基正确（6分）
- 高斯消元正确（4分）
- 最终结果正确（3分）


### 题型2：无约束优化——最速下降法

**出题频率**：★★★★☆
**分值**：12~15分
**难度**：★★★★☆

**【例题2】** 用最速下降法求解 \( \min f(x) = x_1^2 + 2x_2^2 \)，初始点 \( \boldsymbol{x}^{(0)} = (2, 1)^T \)，迭代两步（精确一维搜索）。

**【详解】**

**第一步**：计算梯度：
$$
\nabla f(x) = \begin{pmatrix} 2x_1 \\ 4x_2 \end{pmatrix}
$$

**初始点梯度**：\( \nabla f(\boldsymbol{x}^{(0)}) = \begin{pmatrix} 4 \\ 4 \end{pmatrix} \)

**搜索方向**：\( \boldsymbol{d}^{(0)} = -\nabla f(\boldsymbol{x}^{(0)}) = \begin{pmatrix} -4 \\ -4 \end{pmatrix} \)

**第二步**：精确一维搜索求 α₀。

$$
\phi(\alpha) = f(\boldsymbol{x}^{(0)} + \alpha \boldsymbol{d}^{(0)}) = (2 - 4\alpha)^2 + 2(1 - 4\alpha)^2
$$

$$
= (4 - 16\alpha + 16\alpha^2) + 2(1 - 8\alpha + 16\alpha^2)
$$

$$
= 4 - 16\alpha + 16\alpha^2 + 2 - 16\alpha + 32\alpha^2
$$

$$
= 6 - 32\alpha + 48\alpha^2
$$

求导：\( \phi'(\alpha) = -32 + 96\alpha = 0 \) → \( \alpha_0 = 1/3 \)

**第三步**：计算 \( \boldsymbol{x}^{(1)} \)：

$$
\boldsymbol{x}^{(1)} = \boldsymbol{x}^{(0)} + \alpha_0 \boldsymbol{d}^{(0)} = \begin{pmatrix} 2 \\ 1 \end{pmatrix} + \frac{1}{3} \begin{pmatrix} -4 \\ -4 \end{pmatrix} = \begin{pmatrix} 2/3 \\ -1/3 \end{pmatrix}
$$

**第四步**：计算第二次迭代的梯度：

$$
\nabla f(\boldsymbol{x}^{(1)}) = \begin{pmatrix} 4/3 \\ -4/3 \end{pmatrix}
$$

搜索方向：\( \boldsymbol{d}^{(1)} = -\nabla f(\boldsymbol{x}^{(1)}) = \begin{pmatrix} -4/3 \\ 4/3 \end{pmatrix} \)

**第五步**：一维搜索求 α₁：

$$
f(\boldsymbol{x}^{(1)} + \alpha \boldsymbol{d}^{(1)}) = \left(\frac{2}{3} - \frac{4}{3}\alpha\right)^2 + 2\left(-\frac{1}{3} + \frac{4}{3}\alpha\right)^2
$$

展开：
$$
= \frac{4}{9}(1 - 2\alpha)^2 + 2 \cdot \frac{1}{9}(-1 + 4\alpha)^2
$$

求导令为0：
$$
\frac{4}{9} \cdot 2(1-2\alpha)(-2) + \frac{2}{9} \cdot 2(-1+4\alpha) \cdot 4 = 0
$$

化简：
$$
-\frac{16}{9}(1-2\alpha) + \frac{16}{9}(-1+4\alpha) = 0
$$

$$
-(1-2\alpha) + (-1+4\alpha) = 0
$$

$$
-1 + 2\alpha - 1 + 4\alpha = 0
$$

$$
6\alpha = 2 \rightarrow \alpha_1 = 1/3
$$

**第六步**：计算 \( \boldsymbol{x}^{(2)} \)：

$$
\boldsymbol{x}^{(2)} = \boldsymbol{x}^{(1)} + \alpha_1 \boldsymbol{d}^{(1)} = \begin{pmatrix} 2/3 \\ -1/3 \end{pmatrix} + \frac{1}{3} \begin{pmatrix} -4/3 \\ 4/3 \end{pmatrix} = \begin{pmatrix} 2/3 - 4/9 \\ -1/3 + 4/9 \end{pmatrix} = \begin{pmatrix} 2/9 \\ 1/9 \end{pmatrix}
$$

**答案**：\( \boldsymbol{x}^{(1)} = (2/3, -1/3)^T \)，\( \boldsymbol{x}^{(2)} = (2/9, 1/9)^T \)

**【给分要点】**
- 梯度计算正确（2分）
- 精确一维搜索公式正确（4分）
- α₀ 计算正确（2分）
- x₁ 正确（2分）
- 第二步计算正确（3分）


### 题型3：无约束优化——牛顿法

**出题频率**：★★★★☆
**分值**：12~15分
**难度**：★★★★☆

**【例题3】** 用牛顿法求解 \( \min f(x) = x_1^2 + 4x_2^2 + 2x_1x_2 \)，初始点 \( \boldsymbol{x}^{(0)} = (2, 1)^T \)，迭代一步。

**【详解】**

**第一步**：计算梯度 \( \nabla f(x) \)：

$$
\nabla f(x) = \begin{pmatrix} 2x_1 + 2x_2 \\ 8x_2 + 2x_1 \end{pmatrix}
$$

**初始点梯度**：
$$
\nabla f(\boldsymbol{x}^{(0)}) = \begin{pmatrix} 2(2) + 2(1) \\ 8(1) + 2(2) \end{pmatrix} = \begin{pmatrix} 6 \\ 12 \end{pmatrix}
$$

**第二步**：计算海色矩阵 \( \nabla^2 f(x) \)：

$$
\nabla^2 f(x) = \begin{bmatrix} 2 & 2 \\ 2 & 8 \end{bmatrix}
$$

**第三步**：解线性方程组求牛顿方向 \( \boldsymbol{d} \)：

$$
\nabla^2 f(\boldsymbol{x}^{(0)}) \boldsymbol{d} = -\nabla f(\boldsymbol{x}^{(0)})
$$

$$
\begin{bmatrix} 2 & 2 \\ 2 & 8 \end{bmatrix} \begin{pmatrix} d_1 \\ d_2 \end{pmatrix} = \begin{pmatrix} -6 \\ -12 \end{pmatrix}
$$

解方程：
- 2d₁ + 2d₂ = -6 → d₁ + d₂ = -3
- 2d₁ + 8d₂ = -12 → d₁ + 4d₂ = -6

两式相减：3d₂ = -3 → d₂ = -1
d₁ = -3 - (-1) = -2

**牛顿方向**：\( \boldsymbol{d} = (-2, -1)^T \)

**第四步**：计算 \( \boldsymbol{x}^{(1)} = \boldsymbol{x}^{(0)} + \boldsymbol{d} \)：

$$
\boldsymbol{x}^{(1)} = \begin{pmatrix} 2 \\ 1 \end{pmatrix} + \begin{pmatrix} -2 \\ -1 \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \end{pmatrix}
$$

**验证**：f(0,0) = 0，梯度 ∇f(0,0) = 0，且海色矩阵正定，故 \( \boldsymbol{x}^* = (0,0)^T \) 为全局最优解（二次凸函数，牛顿法一步收敛）。

**【给分要点】**
- 梯度正确（2分）
- 海色矩阵正确（2分）
- 牛顿方向求解正确（4分）
- 迭代结果正确（2分）
- 最优性验证（2分）


### 题型4：约束优化——KKT条件

**出题频率**：★★★★★（每年必考）
**分值**：12~18分
**难度**：★★★★★

**【例题4】** 写出并求解下列优化问题的KKT条件：

$$
\min \quad f(x) = x_1^2 + x_2^2
$$
$$
\text{s.t.} \quad x_1 + x_2 \geq 2
$$
$$
\quad x_1, x_2 \geq 0
$$

**【详解】**

**第一步**：将不等式约束化为标准形式 \( g(x) \leq 0 \)：

- \( g_1(x) = 2 - x_1 - x_2 \leq 0 \)（等价于 \( x_1 + x_2 \geq 2 \)）
- \( g_2(x) = -x_1 \leq 0 \)
- \( g_3(x) = -x_2 \leq 0 \)

**第二步**：写出拉格朗日函数：

$$
L(x_1, x_2, \mu_1, \mu_2, \mu_3) = x_1^2 + x_2^2 + \mu_1(2 - x_1 - x_2) + \mu_2(-x_1) + \mu_3(-x_2)
$$

**第三步**：写出KKT条件：

**平稳性**：
$$
\frac{\partial L}{\partial x_1} = 2x_1 - \mu_1 - \mu_2 = 0
$$
$$
\frac{\partial L}{\partial x_2} = 2x_2 - \mu_1 - \mu_3 = 0
$$

**原始可行性**：
$$
2 - x_1 - x_2 \leq 0, \quad x_1 \geq 0, \quad x_2 \geq 0
$$

**对偶可行性**：
$$
\mu_1 \geq 0, \quad \mu_2 \geq 0, \quad \mu_3 \geq 0
$$

**互补松弛性**：
$$
\mu_1(2 - x_1 - x_2) = 0, \quad \mu_2 x_1 = 0, \quad \mu_3 x_2 = 0
$$

**第四步**：求解KKT系统。

由于目标函数 \( x_1^2 + x_2^2 \) 在约束 \( x_1 + x_2 \geq 2 \) 下最小，几何上是在直线 \( x_1 + x_2 = 2 \) 上找离原点最近的点。

猜测最优解在 \( x_1 > 0, x_2 > 0 \) 上，且约束活跃 \( x_1 + x_2 = 2 \)。

由互补松弛性：\( \mu_2 = 0, \mu_3 = 0 \)

由平稳性：\( 2x_1 = \mu_1, \quad 2x_2 = \mu_1 \)，所以 \( x_1 = x_2 \)

代入约束：\( x_1 + x_2 = 2 \) → \( x_1 = x_2 = 1 \)

于是 \( \mu_1 = 2 \)，满足 \( \mu_1 \geq 0 \)。

**最优解**：\( x_1^* = 1, x_2^* = 1 \)，最优值 \( f^* = 2 \)。

**【给分要点】**
- 约束化为标准形式（2分）
- 拉格朗日函数正确（3分）
- KKT条件完整（5分）
- 解KKT系统（5分）
- 最优值计算（2分）


## 二、★★ 高频小题（约25~30分）


### 题型5：凸性判断

**出题频率**：★★★★☆
**分值**：3~4分/题
**难度**：★★★☆☆

**【例题5】** 判断函数 \( f(x) = x_1^2 + 2x_1x_2 + 4x_2^2 \) 是否为凸函数。

**【详解】**

计算海色矩阵：
$$
\nabla^2 f(x) = \begin{bmatrix} 2 & 2 \\ 2 & 8 \end{bmatrix}
$$

检查主子式：
- 一阶主子式：2 > 0
- 二阶主子式（行列式）：2×8 - 2×2 = 16 - 4 = 12 > 0

所有顺序主子式 > 0，海色矩阵**正定**，故 \( f(x) \) 为**严格凸函数**。

**【例题6】** 判断集合 \( C = \{ (x_1, x_2) \mid x_1^2 + x_2^2 \leq 1 \} \) 是否为凸集。

**【详解】**
单位圆盘（包括内部）是凸集。任意两点在圆内，其连线也在圆内（由三角不等式保证）。
答案：**是凸集**。

**【例题7】** 判断集合 \( S = \{ (x_1, x_2) \mid x_1^2 + x_2^2 = 1 \} \) 是否为凸集。

**【详解】**
单位圆（仅圆周）不是凸集。取两点 \( (1,0) \) 和 \( (-1,0) \)，连线经过原点 \( (0,0) \)，不在圆周上。

答案：**不是凸集**。

**【给分要点】**
- 海色矩阵计算（1分）
- 主子式检验（2分）
- 结论正确（1分）


### 题型6：凸函数判定（一维情形）

**出题频率**：★★★★☆
**分值**：3~4分
**难度**：★★☆☆☆

**【例题8】** 判断函数 \( f(x) = e^x \) 在 \( \mathbb{R} \) 上是否为凸函数。

**【详解】**
f'(x) = e^x，f''(x) = e^x > 0，二阶导数恒大于0，故 f(x) 为**严格凸函数**。

**【例题9】** 判断函数 \( f(x) = x^3 \) 在 \( \mathbb{R} \) 上是否为凸函数。

**【详解】**
f'(x) = 3x²，f''(x) = 6x。当 x < 0 时 f''(x) < 0，当 x > 0 时 f''(x) > 0。二阶导数变号，故 f(x) **不是凸函数**。


### 题型7：线性规划的对偶

**出题频率**：★★★★☆
**分值**：3~4分
**难度**：★★★☆☆

**【例题10】** 写出下列线性规划的对偶问题：

$$
\begin{aligned}
\max \quad & z = 5x_1 + 3x_2 \\
\text{s.t.} \quad & 2x_1 + x_2 \leq 8 \\
& x_1 + 3x_2 \leq 9 \\
& x_1, x_2 \geq 0
\end{aligned}
$$

**【详解】**

该问题为最大化问题，约束为 ≤，变量 ≥ 0。标准形式的对偶是**最小化**问题，对偶变量 y₁ ≥ 0，y₂ ≥ 0。

对偶问题：
$$
\min \quad 8y_1 + 9y_2
$$
$$
\text{s.t.} \quad 2y_1 + y_2 \geq 5 \quad (\text{对应原变量 } x_1)
$$
$$
\quad y_1 + 3y_2 \geq 3 \quad (\text{对应原变量 } x_2)
$$
$$
\quad y_1, y_2 \geq 0
$$


### 题型8：一维搜索的区间缩减

**出题频率**：★★★☆☆
**分值**：4分
**难度**：★★★☆☆

**【例题11】** 用黄金分割法（0.618法）在区间 [0, 2] 上极小化函数 \( \phi(\alpha) = \alpha^2 - 2\alpha \)，迭代一步后区间变为多少？

**【详解】**

**第一步**：初始区间 a₀ = 0，b₀ = 2。

**两个试探点**：
- α₁ = a₀ + 0.382(b₀ - a₀) = 0 + 0.382×2 = 0.764
- α₂ = a₀ + 0.618(b₀ - a₀) = 0 + 0.618×2 = 1.236

**第二步**：计算函数值：
- φ(0.764) = 0.764² - 2×0.764 = 0.584 - 1.528 = -0.944
- φ(1.236) = 1.236² - 2×1.236 = 1.528 - 2.472 = -0.944

**第三步**：比较大小。φ(α₁) = φ(α₂)，当两点函数值相等时，区间两端同时向内收缩：
- 新区间下界 = α₁ = 0.764
- 新区间上界 = α₂ = 1.236

答案：新区间为 **[0.764, 1.236]**。


## 三、★ 中频小题（约15分）


### 题型9：最速下降法步长计算

**出题频率**：★★★☆☆
**分值**：3~4分
**难度**：★★★☆☆

**【例题12】** 已知 \( f(x) = x_1^2 + x_2^2 \)，当前点 \( x^{(k)} = (3, 4)^T \)，搜索方向 \( d^{(k)} = (-1, -1)^T \)，求精确线性搜索的步长 α。

**【详解】**

精确搜索：\( \phi(\alpha) = f(x^{(k)} + \alpha d^{(k)}) = (3 - \alpha)^2 + (4 - \alpha)^2 \)

展开：
φ(α) = (9 - 6α + α²) + (16 - 8α + α²) = 25 - 14α + 2α²

求导：φ'(α) = -14 + 4α = 0 → α = 14/4 = 3.5

答案：**α = 3.5**


### 题型10：一阶与二阶最优性条件

**出题频率**：★★★☆☆
**分值**：3分
**难度**：★★★☆☆

**【例题13】** \( f(x) = x_1^2 + 2x_1x_2 + 3x_2^2 \)，点 \( (0,0) \) 是什么类型的点？

**【详解】**

梯度：
$$
\nabla f(x) = \begin{pmatrix} 2x_1 + 2x_2 \\ 2x_1 + 6x_2 \end{pmatrix}
$$

在 (0,0)：∇f(0,0) = 0，驻点。

海色矩阵：
$$
\nabla^2 f(x) = \begin{bmatrix} 2 & 2 \\ 2 & 6 \end{bmatrix}
$$

一阶主子式：2 > 0；行列式：2×6 - 2×2 = 12 - 4 = 8 > 0。海色矩阵正定。

答案：**严格局部极小值点**（也是全局极小值点，因凸函数）。


### 题型11：对偶变量含义

**出题频率**：★★☆☆☆
**分值**：2分
**难度**：★★☆☆☆

**【例题14】** 在对偶理论中，对偶变量的最优值称为______，其含义是______。

**【详解】**
答案：**影子价格**；含义是**资源单位变化引起目标函数最优值的变化量**。


### 题型12：凸优化重要性质

**出题频率**：★★☆☆☆
**分值**：2分
**难度**：★★☆☆☆

**【例题15】** 凸优化问题的一个核心性质是______。

**【详解】**
答案：**任意局部最优解也是全局最优解**。

> 这是凸优化最重要的性质，也是为什么凸优化在机器学习中如此重要的原因。


## 四、各类题型分值分布速查

| 题型类别 | 典型题型 | 出现频率 | 建议用时 |
|----------|----------|----------|----------|
| ★★★ | 线性规划单纯形法 | 必考（大） | 20~25分钟 |
| ★★★ | 最速下降法 | 必考（大） | 15~20分钟 |
| ★★★ | 牛顿法 | 高频（大） | 15分钟 |
| ★★★ | KKT条件 | 必考（大） | 15~20分钟 |
| ★★ | 凸性判断 | 高频 | 3~5分钟/题 |
| ★★ | 对偶问题 | 高频 | 5分钟/题 |
| ★★ | 一维搜索 | 中频 | 5分钟/题 |
| ★ | 步长计算 | 中频 | 3分钟/题 |
| ★ | 最优性条件 | 中频 | 3分钟/题 |


## 五、解题策略与易错提醒

**🔥 单纯形法**
- 检验数 σ 的计算：\( \sigma_j = c_j - \boldsymbol{c}_B^T B^{-1} \boldsymbol{a}_j \)
- 最小化问题选择**最负检验数**入基；最大化问题选择**最正检验数**入基
- 离基变量：**最小比值规则**（分母必须 > 0）

**🔥 最速下降法**
- 搜索方向：\( \boldsymbol{d}^{(k)} = -\nabla f(\boldsymbol{x}^{(k)}) \)
- 精确搜索求 α：令 \( \phi'(\alpha) = 0 \)
- 收敛慢的原因是**锯齿现象**（相邻方向正交）

**🔥 牛顿法**
- 牛顿方向：\( \boldsymbol{d}^{(k)} = -[\nabla^2 f(\boldsymbol{x}^{(k)})]^{-1} \nabla f(\boldsymbol{x}^{(k)}) \)
- 海色矩阵必须正定（否则需修正）
- 对二次凸函数，**一步收敛**

**🔥 KKT条件**
- 先化约束为标准形式：等式为 = 0，不等式为 ≤ 0
- 平稳性：对拉格朗日函数各变量求导 = 0
- 互补松弛性：\( \mu_j g_j(\boldsymbol{x}) = 0 \)
- 解KKT系统时，通过**猜测哪些约束活跃**来求解

**🔥 凸性判断**
- 凸集判断：任意两点连线在集合内
- 凸函数一阶条件：\( f(y) \ge f(x) + \nabla f(x)^T(y-x) \)
- 凸函数二阶条件：海色矩阵半正定
- **二次型凸性判定**：\( f(x) = x^T Q x \) 为凸函数 ⇔ Q 半正定

---

> 以上为最优化方法核心例题精讲。如需更多同类练习、课后作业解析或华东师大历年考题，可进一步告知。
